#!/usr/bin/env bash
set -euo pipefail
cd -- "$(dirname -- "$0")" && exec > .gitignore
gi() { curl -L -s https://www.gitignore.io/api/"$*"; }
gibo dump Linux
gibo dump Windows
gibo dump macOS
gibo dump Node
echo '.tmp/'
echo 'coverage/'
echo '*.tsbuildinfo'
echo '!dist'
